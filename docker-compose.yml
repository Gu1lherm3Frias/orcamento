services:
  orcamento:
    build: .
    image: orcamento:latest
    container_name: orcamento
    ports:
      - "8000:80"
    depends_on:
      - mariadb
    networks:
      - orcamento-network
    volumes:
      - ./:/var/www/html
    environment:
      HOME: /tmp
    user: "${UID:-1000}:${GID:-1000}"

  mariadb:
    image: mariadb:11
    container_name: orcamento_mariadb
    restart: always
    environment:
      MYSQL_DATABASE: orcamento
      MYSQL_USER: orcamento
      MYSQL_PASSWORD: orcamento
      MYSQL_ROOT_PASSWORD: orcamento
    volumes:
      - mariadb_data:/var/lib/mysql
    networks:
      - orcamento-network
  
  phpmyadmin:
    image: phpmyadmin:latest
    container_name: orcamento_phpmyadmin
    restart: always
    depends_on:
      - mariadb
    ports:
      - "8081:80"
    environment:
      PMA_HOST: mariadb
      PMA_PORT: 3306
      PMA_USER: root
      PMA_PASSWORD: orcamento
    networks:
      - orcamento-network

  selenium:
    image: selenium/standalone-chrome
    container_name: orcamento_selenium
    ports:
      - "7900:7900" # VNC (pra ver o browser rodando)
    networks:
      - orcamento-network
    shm_size: 2gb
    
  senhaunica-faker:
    image: uspdev/senhaunica-faker
    container_name: orcamento_senhaunica-faker
    ports:
      - "3141:3141"
    environment:
          - APP_URL=http://auth.local:3141
    networks:
      orcamento-network:
        aliases:
          - auth.local

  mailpit:
    image: axllent/mailpit:latest
    container_name: orcamento_mailpit
    ports:
      - "1025:1025"   # SMTP
      - "8025:8025"   # Interface Web
    networks:
      - orcamento-network
      
networks:
  orcamento-network:

volumes:
  mariadb_data: